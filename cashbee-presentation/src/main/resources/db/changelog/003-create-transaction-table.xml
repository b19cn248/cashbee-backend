<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="003-create-transaction-table" author="cashbee-team">
        <comment>
            Create transactions table for audit trail and transaction history.
            Every wallet operation creates a transaction record.
        </comment>

        <createTable tableName="transactions">
            <!-- Primary Key -->
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <!-- Foreign Keys -->
            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="wallet_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <!-- Transaction Details -->
            <column name="type" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="amount" type="DECIMAL(19,2)">
                <constraints nullable="false"/>
            </column>

            <column name="description" type="VARCHAR(500)">
                <constraints nullable="true"/>
            </column>

            <!-- Balance Tracking -->
            <column name="balance_before" type="DECIMAL(19,2)">
                <constraints nullable="false"/>
            </column>

            <column name="balance_after" type="DECIMAL(19,2)">
                <constraints nullable="false"/>
            </column>

            <!-- Status -->
            <column name="status" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <!-- Audit Fields -->
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Indexes for Performance -->
        <createIndex indexName="idx_transaction_user_id" tableName="transactions">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_transaction_wallet_id" tableName="transactions">
            <column name="wallet_id"/>
        </createIndex>

        <createIndex indexName="idx_transaction_created_at" tableName="transactions">
            <column name="created_at"/>
        </createIndex>

        <!-- Composite index for user history queries (most common query pattern) -->
        <createIndex indexName="idx_transaction_user_created" tableName="transactions">
            <column name="user_id"/>
            <column name="created_at"/>
        </createIndex>

        <!-- Foreign Key Constraints -->
        <addForeignKeyConstraint
                baseTableName="transactions"
                baseColumnNames="user_id"
                constraintName="fk_transaction_user"
                referencedTableName="user"
                referencedColumnNames="id"
                onDelete="CASCADE"
                onUpdate="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="transactions"
                baseColumnNames="wallet_id"
                constraintName="fk_transaction_wallet"
                referencedTableName="user_wallet"
                referencedColumnNames="id"
                onDelete="CASCADE"
                onUpdate="CASCADE"/>

        <rollback>
            <dropTable tableName="transactions"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
